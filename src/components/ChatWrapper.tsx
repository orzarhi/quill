import React, { FC } from 'react'

interface ChatWrapperProps {

}

export const ChatWrapper: FC<ChatWrapperProps> = ({ }) => {
    return (
        <div>
            ChatWrapper
        </div>
    )
}