import React, { FC } from 'react'

interface MessagesProps {

}

export const Messages: FC<MessagesProps> = ({ }) => {
    return (
        <div>
            Messages
        </div>
    )
}